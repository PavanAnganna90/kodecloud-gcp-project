# Generic Service. Selector matches base Deployment labels.
apiVersion: v1
kind: Service
metadata:
  name: app
  labels:
    app.kubernetes.io/name: app
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
